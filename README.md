# log-analytics-engine
A high-performance Log Analytics Engine built in Python that processes large log files using streaming parsing, O(n) aggregation, and structured reporting. Supports CLI filtering, JSON export, and performance benchmarking. Tested with 1M+ log entries.
